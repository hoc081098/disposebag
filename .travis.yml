language: dart
dart:
  - stable
  - dev
script:
  - dartanalyzer --fatal-infos --fatal-warnings ./
  - dartfmt -n ./lib --set-exit-if-changed
  - pub get
  - pub global activate coverage
  - pub run test test/disposebag_test.dart --coverage=coverage
  - pub global run coverage:format_coverage --in coverage/src/disposebag_base.dart.vm.json --out lcov.info --lcov
after_success:
  - bash <(curl -s https://codecov.io/bash)
cache:
  directories:
    - $HOME/.pub-cache
